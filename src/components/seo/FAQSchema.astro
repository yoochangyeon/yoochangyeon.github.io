---
/**
 * FAQ Schema Component
 *
 * FAQ (Frequently Asked Questions) 형식의 콘텐츠를 Schema.org 구조화된 데이터로 마크업합니다.
 * 트러블슈팅 섹션이나 Q&A 형식의 콘텐츠에 적용하면 검색 결과에 리치 스니펫으로 노출될 수 있습니다.
 *
 * SEO 효과:
 * - Google 검색 결과에 FAQ 리치 스니펫 표시 가능
 * - 검색 결과 클릭률(CTR) 향상
 * - "어떻게", "왜" 등의 질문형 검색어에 유리
 * - Featured Snippet 노출 가능성 증가
 *
 * 사용 예시:
 * <FAQSchema faqs={[
 *   {
 *     question: "NestJS ConfigService에서 값이 조회되지 않는 이유는?",
 *     answer: "ConfigModule을 다른 모듈에서 import하면 DynamicModule이 아닌 기본 ConfigModule이 사용되어..."
 *   },
 *   {
 *     question: "Lambda@Edge AVIF 변환이 느린 이유는?",
 *     answer: "AVIF 변환은 고압축률 알고리즘으로 인해 WebP 대비 8-10배 더 오래 걸립니다..."
 *   }
 * ]} />
 */

export interface FAQ {
  question: string;
  answer: string;
}

export interface Props {
  faqs: FAQ[];
}

const { faqs } = Astro.props;

// FAQPage schema 생성
const schema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((faq) => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: faq.answer,
    },
  })),
};
---

<script type="application/ld+json" set:html={JSON.stringify(schema)} />
